<form method="post" action="{{ path('app_carnet_delete', {'id': carnet.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ carnet.id) }}">
    <button class="btn">Delete</button>
</form>
